# Runtime dependencies for the Assignment 3 scripts on a Google Cloud TPU VM.
# JAX's official TPU install uses the `tpu` extra, which includes the TPU runtime pieces.
jax[tpu]

# Used to inspect traces generated by `silu/silu.py` via `jax.profiler.trace`.
xprof
